package IoStream1;

public class MethodOfFileWriter {
//to write method  1]write(int ch):single character to file-unicode value
	//fw.write(100);d
	//fw.write('d');
	// 1]write(int ch)
	// 2] grp of char  write (char[])
    // 3] string write (string s))
	// 4] flush():to give the garanty abt the last char will enter in file
	// 5] close()
	
}
